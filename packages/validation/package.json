{
  "name": "@remotfix/validation",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"try { require('fs').rmSync('dist', { recursive: true, force: true }); } catch (e) {}\""
  },
  "dependencies": {
    "@remotfix/types": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@remotfix/config": "workspace:*",
    "typescript": "^5.7.3"
  }
}
