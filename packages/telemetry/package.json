{
  "name": "@remotfix/telemetry",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"try { require('fs').rmSync('dist', { recursive: true, force: true }); } catch (e) {}\""
  },
  "devDependencies": {
    "@remotfix/config": "workspace:*",
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  }
}
